package Oriented;

public class Test {
    //这个类没有什么现实意义
    //只是为了将主方法写在这里

    public static void main(String[]args) {
        /*
        //想要在这里使用一个人 来执行操作
        //创建一个人类的对象 找一个变量空间来存储
        Person p = new Person();
        //通过对象 去调用属性 存值/取值
        //属性是有默认值 name==null  age==0  sex==null
        p.name = "陈宇鹏";
        p.age = 18;
        p.sex = "男";
        Person p1 = p;
        p1.name = "王雪";
        p1.age = 18;
        p1.sex = "女";
        System.out.println(p.name+"今年"+p.age+"岁，性别是"+p.sex);
        System.out.println(p1.name+"今年"+p1.age+"岁，性别是"+p1.sex);
        //栈内存中  新的变量空间p1 引用地址改为p的引用地址
        //即新对象p1属性的赋值和取值 会更改堆内存中共用对象的值 而p变量空间的引用地址的值也随之改变


        //方法设计

        //无参数无返回值
        System.out.println();
        Person a = new Person();
        p.name = "陈宇鹏";//属性颜色紫色 没有小括号 用来赋值/取值 =
        p.eat();//方法颜色灰白色 存在一个小括号(传递参数) 执行一次:

        //无参数有返回值
        System.out.println();
        Person b = new Person();
        String myName = b.tellName();
        System.out.println(myName);

        //有参数无返回值
        Person d = new Person();


         */
    }
}
